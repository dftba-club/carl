networks:
  backend:
    external: true
    name: "backend"
services:
  carl:
    container_name: "carl"
    env_file: .env
    hostname: "carl"
    image: "ghcr.io/dftba-club/carl:latest"
    restart: "unless-stopped"
    networks:
      - backend